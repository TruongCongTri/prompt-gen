"use client";
import { LayoutDashboard } from "lucide-react";
import FeaturePlaceholder from "@/components/FeaturePlaceholder"; // Ensure imported

export default function Dashboard() {
  return (
    <FeaturePlaceholder title="Tổng Quan & Thống Kê" icon={LayoutDashboard} />
  );
}